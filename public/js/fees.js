function editFees(date, appartmentId, toPay, payed, balance, comments) {
    alert(date + ", " + appartmentId + ", " + toPay + ", " + payed + ", " + balance + ", " + comments);
    document.getElementById('id01').style.display = 'block';
}